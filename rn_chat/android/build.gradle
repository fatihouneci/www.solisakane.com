buildscript {
    ext {
        buildToolsVersion = "34.0.0"
        minSdkVersion = 24
        compileSdkVersion = 34
        targetSdkVersion = 34
        ndkVersion = "25.1.8937393"
        kotlinVersion = "1.8.0"
        // Configuration pour la compatibilité 16KB
        // Configuration for 16KB compatibility
        enable16KbPageSize = true
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
        classpath 'com.google.gms:google-services:4.4.2'
    }

    subprojects { subproject ->

        if(project['name'] == 'react-native-switch-audio-output'){
            project.configurations { compile { } }
        }
        
        if(project['name'] == 'react-native-incoming-call'){
            project.configurations { compile { } }
        }

        afterEvaluate{
            if((subproject.plugins.hasPlugin('android') || subproject.plugins.hasPlugin('android-library'))) {
                android {
                    compileSdkVersion rootProject.ext.compileSdkVersion
                    buildToolsVersion rootProject.ext.buildToolsVersion
                }
            }
        }

        
    }
}

allprojects {
    repositories {
      mavenLocal()
      google()
      mavenCentral()
      maven { url "$rootDir/../node_modules/react-native/android" }

      // Repositories pour Notifee et autres modules
      maven { url 'https://maven.google.com' }
      maven { url "https://www.jitpack.io" }
      maven { url "https://oss.sonatype.org/content/repositories/snapshots/" }
      
      // Repository pour Notifee spécifiquement
      maven { url "https://developer.huawei.com/repo/" }
    }
}

apply plugin: "com.facebook.react.rootproject"
