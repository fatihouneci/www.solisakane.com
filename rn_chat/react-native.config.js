/**
 * Configuration React Native pour désactiver temporairement certains modules
 * React Native configuration to temporarily disable some modules
 */

module.exports = {
  dependencies: {
    // Configuration des modules React Native
    // React Native modules configuration
  },
  assets: ['./src/assets/fonts/', './src/assets/images/'],
};
